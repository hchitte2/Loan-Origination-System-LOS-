## Phase

Phase N — <title> (see PLAN.md §9). One PR per phase; small conventional commits inside it.

## What changed

- 

## Done-when checklist (copy from the phase block; tick only what you verified)

- [ ] 

## How it was verified

- `/verify` result (biome, tsc, unit, db, e2e + axe in both themes):
- Browser check as each affected role on the preview URL:
- Reviewers run: `authz-reviewer` / `ui-reviewer` / `schema-guard` → PASS or fixes applied

## Screenshots (UI phases, light and dark)

## Drift from PLAN.md or a skill

None / describe, so the planning chat can update the plan.
