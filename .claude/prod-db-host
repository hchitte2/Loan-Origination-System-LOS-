# Hostname(s) of the Neon *main* (production) branch, one per line, e.g.
#   ep-quiet-forest-123456.us-east-2.aws.neon.tech
# Not a secret: a hostname alone grants nothing. Lines starting with # and blank lines are ignored.
# .claude/hooks/block-unsafe.sh denies any Bash command that contains a listed host unless the
# command starts with CLEARLINE_RELEASE=1 (used only by the /release skill).
# Fill this in during Phase 0 once the Neon project exists.
